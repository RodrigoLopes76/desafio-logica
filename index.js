//var nickname;
//nickname = prompt("Digite o nome do Herói: ")

//var XP;
//XP = prompt("Digite quanto XP o seu Herói possui: ")
let nickname = "Pikatchu"
let xp = 1500

switch (xp){
    case xp < 1000:
        console.log(`O Herói de nome ${nickname.concat( )} está no nível de Ferro`)
        
    case 1001 <= xp < 2000:
        console.log("O Herói de nome ") +nickname ("está no nível de Bronze")
    case 2001 <= xp < 5000:
        console.log("O Herói de nome ") +nickname ("está no nível de Prata")
    case 6001 <= xp < 7000:
        console.log("O Herói de nome ") +nickname ("está no nível de Ouro")
    case 7001 <= xp < 8000:
        console.log("O Herói de nome ") +nickname ("está no nível de Platina")
    case 8001 <= xp < 9000:
        console.log("O Herói de nome ") +nickname ("está no nível de Ascendente")
    case 9001 <= xp < 10000:
        console.log("O Herói de nome ") +nickname ("está no nível de Imortal")
    case xp > 10001:
        console.log("O Herói de nome ") +nickname ("está no nível de Radiante")

}
console.log(`O Herói de nome ${nickname} tem XP de Ouro`);

let nickname = 
let xp = 2002

if (xp < 1000){
        console.log("O Herói de nome Ouro ") ;
    }else if (1001 <= xp < 2000){
        console.log("O Herói de nome Prata ") ;
    }else if (2001 <= xp < 5000){
        console.log("O Herói de nome Bronze") ;
    }
